<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.company.swurameal.dao.mybatis.GoodsDao">

	<!-- <select id="selectByGoodsId" parameterType="int" resultType="Goods"> 
		select goodsId, goodsName, category, subcategory, price from goods where 
		goodsId = #{goodsId} </select> -->
	
	<!-- 특정 상품 가져오기 -->
	<select id="selectByGoodsID" resultType="Goods"
		parameterType="int">
		SELECT
		GOODS_ID,
		GOODS_NAME,
		CATEGORY,
		SUBCATEGORY,
		PRICE,
		GOODS_COMMENT
		FROM goods
		WHERE GOODS_ID = #{goodsId}
	</select>


	<!-- 상품 전체 가져오기 -->
	<select id="selectAllGoods" resultType="Goods"
		parameterType="int">
		SELECT
		GOODS_ID,
		GOODS_NAME,
		CATEGORY,
		SUBCATEGORY,
		PRICE,
		GOODS_COMMENT
		FROM goods
	</select>

	<!-- 상품 이미지 가져오기 -->
	<select id="selectAttachByGoodsID" resultType="GoodsImg"
		parameterType="int">
		SELECT
			GOODS_IMG_ID,
			GOODS_ID,
			GOODS_NAME,
			G_ATTACH_ONAME,
			G_ATTACH_TYPE,
			G_ATTACH_DATA
		FROM goods_img
		WHERE GOODS_ID = #{goodsId}
	</select>
	

</mapper>

